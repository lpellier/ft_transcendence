version: '3.8'

services:

  serv:
    image: node:17.5
    command: sh -c "npm install"
    ports:
      - 3000:3000
    working_dir: /serv
    volumes:
      - ./:/serv
    environment:
  
  db:
    image: postgres:14.2
    environment:
      - POSTGRES_PASSWORD=secret
    volumes:
      - postgresdata:/var/lib/postgresql/data

volumes:
  postgres-data: